//Use in some of the tests

// import { assert } from "chai";

// export function assertUserData(responseData: any, expectedUserData: any): void {
//   assert.equal(responseData.name, expectedUserData.name);
//   assert.equal(responseData.job, expectedUserData.job);
//   if (expectedUserData.id !== undefined) {
//     assert.isString(responseData.id);
//   }
//   if (expectedUserData.createdAt !== undefined) {
//     assert.isString(responseData.createdAt);
//   }
// }
